name: apollo_sdk
description: Apollo AI SDK for Flutter - Reusable AI chat widget and service for Delt, Atlas, and Akashic
version: 1.0.0
homepage: https://github.com/ColossalCapital/Apollo

environment:
  sdk: '>=2.19.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
